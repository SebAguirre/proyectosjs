let resultado = (3>2) ? "verdadero" : "falso";

console.log(resultado);

//condicion ? verdadero : falso

//si la condicion se cumple pasa por la primera expresion
//si no se cumple se ejecuta la expresion 2 
//en cualquiera de los dos casos, la expresion que de el resultado le asignara el valor a la variable
//en este caso el valor de resultado pasa a ser "verdadero"

let numero = 9;

resultado = (numero % 2 == 0) ? "El numero es par" : "Numero Impar";

console.log(resultado);

